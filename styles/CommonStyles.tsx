import { StyleSheet } from "react-native";

const commonStyles = StyleSheet.create({
  textInput: {
    minWidth: "50%",
    height: 40,
    margin: 12,
    borderWidth: 1,
    padding: 10,
  },
  inputLabel: {
    fontSize: 20,
  },
});
export default commonStyles;
